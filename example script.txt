emitter NameHere
{
    int x;
    int y;
    float z;
    
    state InitialState
    {
        function void Run()
        {
            for(int variableName = 0; variableName < 50; variableName++) {
                FireBullet(X, Y, Angle, 500, 500);
                Wait(0.03);
            }
            
            SetState(NextState);
        }
    }
    
    state NextState
    {
    }
    
    Initialize
    {
        SetState(InitialState);
    }    
}