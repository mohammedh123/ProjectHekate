action UpdateBullet()
{
    var angle = 0;
    for(var i = 0; i < 30; i++) {
        angle = i*2*PI/30;
    
        fire bullet($X, $Y, $Angle + angle, $Speed, $SpriteIndex);
        wait(0);
    }

    wait(60);
}