action UpdateBullet()
{
    var angle = 0;
    for(var i = 0; i < 50; i++) {
        angle = GetRandomInt(1,50)*PI_180;
    
        $Angle += angle;

        fire bullet($X, $Y, $Angle + PI_180, $Speed, $SpriteIndex);

        wait(i);
    }
}